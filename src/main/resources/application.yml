spring:
  profiles:
    active: local
---
spring:
  profiles: dev
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/dokotlin
    username: sa
    password:

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show-sql: false
        format_sql: true
    open-in-view: false
logging:
  level:
    # org.hibernate.SQL: debug
    com.yolo.dokotlin: debug
    # org.hibernate.type: trace

---
spring:
  profiles: local
  datasource:
    url: jdbc:mariadb://192.168.0.4:3306/dokotlin
    driver-class-name: org.mariadb.jdbc.Driver
    username: junyoung
    password: wnsdud@123

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    # org.hibernate.SQL: debug
    com.yolo.dokotlin: debug
    # org.hibernate.type: trace
