spring:
  profiles:
    active: local
---
spring:
  profiles: dev
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/dokotlin
    username: sa
    password:

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show-sql: false
        format_sql: true
    open-in-view: false
logging:
  level:
    # org.hibernate.SQL: debug
    com.yolo.dokotlin: debug
    # org.hibernate.type: trace

---
spring:
  profiles: local
  datasource:
    url: jdbc:mariadb://localhost:3306/dokotlin
    driver-class-name: org.mariadb.jdbc.Driver
    username: junyoung
    password: wnsdud@123


  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

management:
  metrics:
    export:
      datadog:
        enabled: true
        api-key: e5580c4e1478158aa3a759395cc6e47c
        application-key: 76520e65af732106fa615986bc6b495d758b933a
        host-tag: thirdparty-example
        step: PT10S

kafka:
  hosts: localhost:9092

logging:
  level:
    # org.hibernate.SQL: debug
    com.yolo.dokotlin: debug
    # org.hibernate.type: trace

